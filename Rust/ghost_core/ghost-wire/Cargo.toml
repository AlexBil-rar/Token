[package]
name = "ghost-wire"
version = "0.1.0"
edition = "2021"

[dependencies]
ledger = { path = "../ledger" }
ghost-params = { path = "../ghost-params" }
serde = { version = "1", features = ["derive"] }
bincode = "1.3"
hex = "0.4"
serde_json = "1"